import { StyledHeader, Nav } from "./styles/Header.styled";

export const Header = () => {
  return (
    <StyledHeader>
      <Nav>
        <h2> Mendelian Inheritance </h2>
      </Nav>
    </StyledHeader>
  );
};
